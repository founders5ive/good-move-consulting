import type { Metadata } from "next";
export const metadata: Metadata = { title: "Field Notes | Good Move Consulting", description: "Observations on organizational capability, operating friction, and the patterns that shape execution." };
export default function FieldNotesLayout({children}:{children:React.ReactNode}) { return children; }
